n=71
SSEG	SEGMENT	STACK
STK	DB	100 DUP(0)
SSEG	ENDS
DSEG	SEGMENT
DSEG	ENDS
CSEG	SEGMENT
	ASSUME	CS:CSEG,DS:DSEG
	ASSUME	SS:SSEG
	MOV	BP,SP
	MOV	DI,SS:[BP+2]
	RET
	ENDP
	MOV	BP,SP
	MOV	AX,SS:[BP+4+0H]
	MOV	BX,0H
	CMP	AX,BX
	JNZ		MOV	ES:[0H],01H
	JMP	:	MOV	ES:[0H],00H
	MOV	AX,SS:[BP+4+0H]
	MOV	BX,1H
	CMP	AX,BX
	JNZ		MOV	ES:[2H],01H
	JMP	:	MOV	ES:[2H],00H
	MOV	AX,ES:[0H]
	OR	AX,ES:[2H]
	JZ		MOV	ES:[4H],01H
	JMP	:	MOV	ES:[4H],00H
	MOV	AX,ES:[4H]
	AND	AX,AX
	JZ		MOV	AL,1H
	MOV	DI,SS:[BP+2]
	RET
	MOV	AX,SS:[BP+4+0H]
	MOV	BX,1H
		AX,BX
	MOV	ES:[6H],AX
	MOV	AX,ES:[6H]
	PUSH	AX
	PUSH	DI
	ADD	DI,4H
	CALL	
	POP	 DX
	POP	 DX
	MOV	BP,SP
	MOV	ES:[8H],AX
	MOV	BX,ES:[8H]
	MOV	[DI+2H],BX
	MOV	AX,SS:[BP+4+0H]
	MOV	BX,2H
		AX,BX
	MOV	ES:[0AH],AX
	MOV	AX,ES:[0AH]
	PUSH	AX
	PUSH	DI
	ADD	DI,4H
	CALL	
	POP	 DX
	POP	 DX
	MOV	BP,SP
	MOV	ES:[0CH],AX
	MOV	BX,ES:[0CH]
	MOV	[DI+0H],BX
	MOV	AX,[DI+2H]
	MOV	BX,[DI+0H]
		AX,BX
	MOV	ES:[0EH],AX
	MOV	AX,ES:[0EH]
	MOV	DI,SS:[BP+2]
	RET
	ENDP
	MOV	AX,CSEG
	ADD	AX,7D0H
	MOV	DS,AX
	ADD	AX,7D0H
	MOV	ES,AX
	ADD	AX,7D0H
	MOV	SS,AX
	MOV	DI,0H
	MOV	BX,5H
	MOV	[DI+0H],BX
	MOV	AX,[DI+0H]
	MOV	BX,0AH
	CMP	AX,BX
	JAE		MOV	ES:[10H],01H
	JMP	:	MOV	ES:[10H],00H
	MOV	AX,ES:[10H]
	AND	AX,AX
	JZ		JMP		MOV	AX,[DI+0H]
		AX
	MOV	[DI+0H],AX
	JMP		MOV	AX,[DI+0H]
	PUSH	AX
	PUSH	DI
	ADD	DI,4H
	CALL	
	POP	 DX
	POP	 DX
	MOV	BP,SP
	MOV	ES:[12H],AX
	MOV	BX,ES:[12H]
	MOV	[DI+2H],BX
	MOV	AX,[DI+2H]
	PUSH	AX
	PUSH	DI
	ADD	DI,4H
	CALL	
	POP	 DX
	POP	 DX
	MOV	BP,SP
	JMP		MOV	AH,4CH
	INT	21H
CSEG	ENDS
END main
