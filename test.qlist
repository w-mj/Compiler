0# (nop, _, _, _)
1# (func, printi, _, _)
2# (ASM,     MOV DX, SS:[BP+4] , _, _)
3# (ASM,     xor  ah, ah , _, _)
4# (ASM,     mov  al, dl , _, _)
5# (ASM,     mov  dh, 10 , _, _)
6# (ASM,     div  dh , _, _)
7# (ASM,     test al, 0ffh , _, _)
8# (ASM,     jz   single , _, _)
9# (ASM,     push ax , _, _)
10# (ASM,     xor  ah, ah , _, _)
11# (ASM,     div  dh , _, _)
12# (ASM,     test al, 0ffh , _, _)
13# (ASM,     jz   two , _, _)
14# (ASM,     push ax , _, _)
15# (ASM,     mov  dl, al , _, _)
16# (ASM,     add  dl, '0' , _, _)
17# (ASM,     mov  ah, 02h , _, _)
18# (ASM,     int  21h , _, _)
19# (ASM,     pop  ax , _, _)
20# (ASM,     two: , _, _)
21# (ASM,     mov  dl, ah , _, _)
22# (ASM,     add  dl, '0' , _, _)
23# (ASM,     mov  ah, 02h , _, _)
24# (ASM,     int  21h , _, _)
25# (ASM,     pop  ax , _, _)
26# (ASM,     single: , _, _)
27# (ASM,     mov  dl, ah , _, _)
28# (ASM,     add  dl, '0' , _, _)
29# (ASM,     mov  ah, 02h , _, _)
30# (ASM,     int  21h , _, _)
31# (ASM,     mov dl, 0ah , _, _)
32# (ASM,     int 21h , _, _)
33# (ret, _, _ , _ )
34# (end-func, printi, _, _)
35# (func, fib, _, _)
36# (def-var, b, _, _)
37# (def-var, a, _, _)
38# (==, @func_1_param_0, @const_0, @0_)
39# (==, @func_1_param_0, @const_1, @1_)
40# (||, @0_, @1_, @2_)
41# (if, @2_, _ , 43)
42# (retn, @const_1, _, _)
43# (end-if, _, _, _)
44# (-, @func_1_param_0, @const_1, @3_)
45# (push, @3_, _ , _)
46# (call, fib, _ , @4_)
47# (=, @4_, _ , a)
48# (-, @func_1_param_0, @const_2, @5_)
49# (push, @5_, _ , _)
50# (call, fib, _ , @6_)
51# (=, @6_, _ , b)
52# (+, a, b, @7_)
53# (retn, @7_, _, _)
54# (end-func, fib, _, _)
55# (func, main, _, _)
56# (def-var, i, _, _)
57# (def-var, x, _, _)
58# (=, @const_5, _ , i)
59# (for, _, _, _)
60# (<, i, @const_10, @8_)
61# (do, @8_, _, 71)
62# (jmp, _, _, 65)
63# (++, i, _ , i)
64# (jmp, _, _, 59)
65# (push, i, _ , _)
66# (call, fib, _ , @9_)
67# (=, @9_, _ , x)
68# (push, x, _ , _)
69# (call, printi, _ , @10_)
70# (end-for, _, _, 63)
71# (end-func, main, _, _)


