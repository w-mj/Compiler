
void printi(int a) {
#asm
    MOV AH, 0
    MOV AL, 18H
    LABEL:
    PUSH AL
    INT 21H
#endasm
    goto LABEL;
}

int fibonacci(int x) {
    if (x == 1 || x == 2)
        return 1;
    return fibonacci(x - 1) + fibonacci(x - 2);
}

struct Stru {
    int a, f;
    char b;
    long c, d, e;
};

int main(void) {
    int a[10], i;
    int b = 1;

    a[3] = b;
    a[3] = a[1] + a[2];

    for (i = 0; i < 10; i++) {
        printi(fibonacci(i));
    }
    return i;
}