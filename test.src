int ext1;
int ext2, ext3;

int printi(int a);

int fibonacci(int x) {
    if (x == 1 || x == 2)
        return 1;
    return fibonacci(x - 1) + fibonacci(x - 2);
}

int main(int i) {
    for (i = 0; i < 10; i++) {
        printi(fibonacci(i));
    }
}